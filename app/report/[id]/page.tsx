import Link from "next/link";
import { notFound } from "next/navigation";
import { createClient, hasSupabaseConfig } from "../../../lib/supabase/server";
import ReportActions from "../../../components/report-actions";

type ReportContent = { scorecard?: { market?: number; pain?: number; differentiation?: number; economics?: number; execution?: number }; summary?: string; market?: string; customer?: string; problem?: string; competitors?: string; gap?: string; businessModel?: string; pricing?: string; risks?: string[]; mvp?: string[]; avoid?: string[]; firstCustomers?: string[]; plan7?: string[]; plan30?: string[]; assumptions?: string[]; sources?: string[] };
type StoredReport = { title: string; verdict: string; score: number; report: ReportContent; created_at: string };
const demo: StoredReport = { title: "Local service growth copilot", verdict: "TEST FIRST", score: 74, created_at: new Date().toISOString(), report: { summary: "A focused growth assistant for independent home-service businesses has a credible wedge: owners need more predictable enquiries but lack the time and specialist support to run marketing systems.", market: "AI estimate: independent home-service operators are a reachable niche with recurring lead-generation pressure.", customer: "Owner-operators who manage jobs and marketing themselves.", problem: "Owners lose leads because follow-up, review collection and local visibility are inconsistent.", competitors: "Broad marketing suites, agencies and manual spreadsheet-based follow-up.", gap: "Existing tools are broad marketing suites or disconnected AI writers.", businessModel: "A subscription with an assisted onboarding pilot can test willingness to pay.", pricing: "Assumption: validate a monthly price with five founder interviews before setting a rate.", risks: ["Weak repeat usage after the first marketing plan", "Generic AI tools may feel sufficient"], mvp: ["Weekly growth plan built from a business profile", "Lead follow-up sequences for email and SMS", "Review request and local-content generator", "A simple results dashboard"], avoid: ["Broad platform features", "Premature automation"], firstCustomers: ["Interview 10 operators", "Offer a concierge pilot", "Ask for a paid commitment"], plan7: ["Write a narrow owner-operator hypothesis", "Book five customer conversations", "Document current workarounds", "Create a simple offer page", "Show the prototype to interviewees", "Ask for a paid pilot", "Decide what to build next"], plan30: ["Complete 15 interviews", "Run three paid pilots", "Measure repeat usage", "Make a build, pivot or stop decision"], assumptions: ["The problem happens often", "Owners have a budget for improved lead handling"], sources: ["AI-generated directional analysis", "Founder interviews required"] } };
const agents = [["Mira", "Market intelligence", "MAP"], ["Asha", "Customer reality", "CUST"], ["Theo", "Competitive terrain", "COMP"], ["Owen", "Business model", "MODEL"], ["Rhea", "Risk review", "RISK"], ["Nova", "MVP & execution", "MVP"]];

export default async function Report({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  let report: StoredReport | null = id === "demo" ? demo : null;
  if (!report && hasSupabaseConfig()) {
    const supabase = await createClient();
    const { data: claims } = await supabase.auth.getClaims();
    if (claims?.claims?.sub) { const { data } = await supabase.from("reports").select("title,verdict,score,report,created_at").eq("id", id).maybeSingle(); report = data as StoredReport | null; }
  }
  if (!report) notFound();
  const content = report.report;
  const verdictLine = report.verdict === "BUILD" ? "A strong opportunity, with a clear reason to pursue the next validation step." : report.verdict === "AVOID" ? "The current risks outweigh the likely opportunity." : "Promising direction, but earn the right to build through customer evidence.";
  const plan = content.plan7?.length ? content.plan7 : ["Write one target-customer hypothesis", "Book five customer conversations", "Create a simple offer page", "Ask for one paid pilot"]; 
  const scorecard = content.scorecard ?? { market: Math.min(88, report.score + 4), pain: Math.min(90, report.score + 7), differentiation: Math.max(25, report.score - 12), economics: Math.max(30, report.score - 4), execution: Math.max(25, report.score - 8) }; const dimensions = [["Market", scorecard.market], ["Customer pain", scorecard.pain], ["Differentiation", scorecard.differentiation], ["Economics", scorecard.economics], ["Execution", scorecard.execution]] as const;
  return <main className="app-shell report-shell"><header className="app-nav"><Link className="brand" href="/"><span className="brand-mark">✦</span> AI Co-Founder</Link><div><Link href="/reports">My reports</Link><Link href="/new">New idea</Link></div></header><section className="report"><div className="report-top"><div><Link className="back" href="/reports">← All reports</Link><div className="eyebrow"><span></span> CO-FOUNDER DECISION BRIEF</div><h1>{report.title}</h1><p>Generated {new Intl.DateTimeFormat("en", { dateStyle: "medium" }).format(new Date(report.created_at))} · Six specialist perspectives</p></div><ReportActions plan={plan} mode="export"/></div><div className="verdict-row"><div className="score"><small>BUSINESS SCORE</small><strong>{report.score}</strong><span>/100</span></div><div className="verdict"><small>RECOMMENDATION</small><h2><i></i>{report.verdict}</h2><p>{verdictLine}</p></div><div className="confidence"><small>RESEARCH STATUS</small><b>{content.sources?.[0]?.includes("AI-generated") ? "AI directional" : "Mixed evidence"}</b><p>Claims are labelled to separate evidence from estimates.</p></div></div><section className="scorecard"><div><span>DIRECTIONAL SCORECARD</span><h2>What is carrying the decision?</h2><p>These are AI estimates based only on the idea submitted—not verified market facts.</p></div><div className="score-bars">{dimensions.map(([label, value]) => <div key={label}><span>{label}</span><b>{value}</b><i><em style={{ width: `${value}%` }}/></i></div>)}</div></section><section className="agent-strip"><div><span>THE ROOM</span><h2>Six specialists reviewed this idea.</h2></div><div className="agent-chips">{agents.map(([name, role, code], index) => <article className={`agent-chip agent-${index}`} key={name}><b>{code}</b><div><strong>{name}</strong><small>{role}</small></div></article>)}</div></section><div className="report-grid"><div className="report-main"><ReportBlock tag="EXECUTIVE SUMMARY" title="The clearest path forward." text={content.summary}/><section className="decision-frame"><div><span>DECISION GATE</span><h2>What must be true before you build?</h2><ol>{(content.assumptions ?? []).map((item, index) => <li key={item}><b>0{index + 1}</b><p>{item}</p></li>)}</ol></div><div><span>WHAT WOULD CHANGE THIS VERDICT</span><h3>Evidence to collect now</h3><ul>{(content.firstCustomers ?? []).map(item => <li key={item}>→ {item}</li>)}</ul></div></section><div className="two"><ReportBlock tag="MARKET OPPORTUNITY" title="Where the signal may be." text={content.market}/><ReportBlock tag="TARGET CUSTOMER" title="Who needs this most." text={content.customer}/></div><div className="two"><ReportBlock tag="CUSTOMER PROBLEM" title="A job that won’t wait." text={content.problem}/><ReportBlock tag="COMPETITIVE GAP" title="Win by being specific." text={content.gap}/></div><div className="two"><ReportBlock tag="COMPETITIVE TERRAIN" title="What they use today." text={content.competitors}/><ReportBlock tag="BUSINESS MODEL" title="How it could work." text={content.businessModel || content.pricing}/></div><ReportBlock tag="RECOMMENDED MVP" title="Build the smallest proof engine." list={content.mvp}/><ReportBlock tag="RISKS TO RETIRE" title="Do not ignore these." list={content.risks}/><ReportBlock tag="FEATURES TO AVOID" title="Keep the first version sharp." list={content.avoid}/><ReportBlock tag="30-DAY EXECUTION PLAN" title="Turn learning into momentum." list={content.plan30}/></div><aside><div className="side-card"><span>YOUR NEXT MOVE</span><h3>Talk to 10 customers this week.</h3><p>Ask about their current workaround, what it costs them, and what would make them switch.</p><ReportActions plan={plan} mode="plan"/></div><div className="source-card"><span>RESEARCH TRACE</span><p><i className="verified"></i> Verified information</p><p><i className="estimate"></i> AI estimates</p><p><i className="assume"></i> Assumptions</p><hr/>{(content.sources ?? []).map(source => <small key={source}>↗ {source}</small>)}</div></aside></div></section></main>;
}
function ReportBlock({ tag, title, text, list }: { tag: string; title: string; text?: string; list?: string[] }) { return <article className="block"><span>{tag}</span><h2>{title}</h2>{text && <p>{text}</p>}{list && <ul>{list.map(item => <li key={item}><b>✓</b>{item}</li>)}</ul>}<small className="data-tag">AI ESTIMATE</small></article>; }
